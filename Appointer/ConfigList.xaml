<Window x:Class="Appointer.ConfigList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактор коллекции строк" Height="600" Width="800" WindowStartupLocation="CenterScreen" >
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Введите строки в коллекцию (одну на сроку):" Padding="0,0,0,5"/>
        <TextBlock Name="Description" Grid.Row="1" TextWrapping="Wrap" Padding="0,0,0,5"/>
        <TextBox Grid.Row="2" Name="Box" AcceptsReturn="True" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible"/>
        <Button Grid.Row="3" Content="Сохранить" Margin="0,10,0,0" Click="Save"/>
    </Grid>
</Window>
